# Contributing

- Haz un commit por historia.
- Usa mensajes en formato `sprint-N: descripcion`.
- Todo cambio en `core/` debe venir con tests.
- Antes de hacer commit, el codigo debe pasar `ruff`, `black`, `mypy core/` y `pytest`.
